<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cl.learn.redis.model.mapper.ArticleMapper">

    <resultMap type="com.cl.learn.redis.model.entity.Article" id="ArticleMap">
        <result property="id" column="id" jdbcType="INTEGER"/>
        <result property="title" column="title" jdbcType="VARCHAR"/>
        <result property="content" column="content" jdbcType="VARCHAR"/>
        <result property="userId" column="user_id" jdbcType="INTEGER"/>
        <result property="scanTotal" column="scan_total" jdbcType="INTEGER"/>
        <result property="praiseTotal" column="praise_total" jdbcType="INTEGER"/>
        <result property="isActive" column="is_active" jdbcType="INTEGER"/>
        <result property="createTime" column="create_time" jdbcType="TIMESTAMP"/>
        <result property="updateTime" column="update_time" jdbcType="TIMESTAMP"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="ArticleMap">
        select
          id, title, content, user_id, scan_total, praise_total, is_active, create_time, update_time
        from sb_redis.article
        where id = #{id}
    </select>

    <!--查询指定行数据-->
    <select id="queryAllByLimit" resultMap="ArticleMap">
        select
          id, title, content, user_id, scan_total, praise_total, is_active, create_time, update_time
        from sb_redis.article
        limit #{offset}, #{limit}
    </select>

    <!--通过实体作为筛选条件查询-->
    <select id="queryAll" resultMap="ArticleMap">
        select
          id, title, content, user_id, scan_total, praise_total, is_active, create_time, update_time
        from sb_redis.article
        <where>
            <if test="id != null">
                and id = #{id}
            </if>
            <if test="title != null and title != ''">
                and title = #{title}
            </if>
            <if test="content != null and content != ''">
                and content = #{content}
            </if>
            <if test="userId != null">
                and user_id = #{userId}
            </if>
            <if test="scanTotal != null">
                and scan_total = #{scanTotal}
            </if>
            <if test="praiseTotal != null">
                and praise_total = #{praiseTotal}
            </if>
            <if test="isActive != null">
                and is_active = #{isActive}
            </if>
            <if test="createTime != null">
                and create_time = #{createTime}
            </if>
            <if test="updateTime != null">
                and update_time = #{updateTime}
            </if>
        </where>
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="id" useGeneratedKeys="true">
        insert into sb_redis.article(title, content, user_id, scan_total, praise_total, is_active, create_time, update_time)
        values (#{title}, #{content}, #{userId}, #{scanTotal}, #{praiseTotal}, #{isActive}, #{createTime}, #{updateTime})
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update sb_redis.article
        <set>
            <if test="title != null and title != ''">
                title = #{title},
            </if>
            <if test="content != null and content != ''">
                content = #{content},
            </if>
            <if test="userId != null">
                user_id = #{userId},
            </if>
            <if test="scanTotal != null">
                scan_total = #{scanTotal},
            </if>
            <if test="praiseTotal != null">
                praise_total = #{praiseTotal},
            </if>
            <if test="isActive != null">
                is_active = #{isActive},
            </if>
            <if test="createTime != null">
                create_time = #{createTime},
            </if>
            <if test="updateTime != null">
                update_time = #{updateTime},
            </if>
        </set>
        where id = #{id}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete from sb_redis.article where id = #{id}
    </delete>

</mapper>