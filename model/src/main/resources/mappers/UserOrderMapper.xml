<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cl.learn.redis.model.mapper.UserOrderMapper">

    <resultMap type="com.cl.learn.redis.model.entity.UserOrder" id="UserOrderMap">
        <result property="id" column="id" jdbcType="INTEGER"/>
        <result property="userId" column="user_id" jdbcType="INTEGER"/>
        <result property="orderNo" column="order_no" jdbcType="VARCHAR"/>
        <result property="payStatus" column="pay_status" jdbcType="INTEGER"/>
        <result property="isActive" column="is_active" jdbcType="INTEGER"/>
        <result property="orderTime" column="order_time" jdbcType="TIMESTAMP"/>
        <result property="updateTime" column="update_time" jdbcType="TIMESTAMP"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="UserOrderMap">
        select
          id, user_id, order_no, pay_status, is_active, order_time, update_time
        from sb_redis.user_order
        where id = #{id}
    </select>

    <!--查询指定行数据-->
    <select id="queryAllByLimit" resultMap="UserOrderMap">
        select
          id, user_id, order_no, pay_status, is_active, order_time, update_time
        from sb_redis.user_order
        limit #{offset}, #{limit}
    </select>

    <!--通过实体作为筛选条件查询-->
    <select id="queryAll" resultMap="UserOrderMap">
        select
          id, user_id, order_no, pay_status, is_active, order_time, update_time
        from sb_redis.user_order
        <where>
            <if test="id != null">
                and id = #{id}
            </if>
            <if test="userId != null">
                and user_id = #{userId}
            </if>
            <if test="orderNo != null and orderNo != ''">
                and order_no = #{orderNo}
            </if>
            <if test="payStatus != null">
                and pay_status = #{payStatus}
            </if>
            <if test="isActive != null">
                and is_active = #{isActive}
            </if>
            <if test="orderTime != null">
                and order_time = #{orderTime}
            </if>
            <if test="updateTime != null">
                and update_time = #{updateTime}
            </if>
        </where>
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="id" useGeneratedKeys="true">
        insert into sb_redis.user_order(user_id, order_no, pay_status, is_active, order_time, update_time)
        values (#{userId}, #{orderNo}, #{payStatus}, #{isActive}, #{orderTime}, #{updateTime})
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update sb_redis.user_order
        <set>
            <if test="userId != null">
                user_id = #{userId},
            </if>
            <if test="orderNo != null and orderNo != ''">
                order_no = #{orderNo},
            </if>
            <if test="payStatus != null">
                pay_status = #{payStatus},
            </if>
            <if test="isActive != null">
                is_active = #{isActive},
            </if>
            <if test="orderTime != null">
                order_time = #{orderTime},
            </if>
            <if test="updateTime != null">
                update_time = #{updateTime},
            </if>
        </set>
        where id = #{id}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete from sb_redis.user_order where id = #{id}
    </delete>

</mapper>